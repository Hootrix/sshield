## 任务

- [x] 在 `internal/core/notify/command.go` 中为含默认值的参数帮助信息补充默认值说明。
- [x] 运行 `gofmt`。
- [x] 视情况提交包含变更的 git commit。
- [x] 在 `internal/core/notify/watcher.go` 中为 `journalRecord` 添加作用说明注释。
- [x] 统一通知配置文件路径逻辑，改为固定使用用户配置目录。
- [x] 更新 README 中的配置路径说明，反映统一后的目录。
- [x] 将 `watch`/`sweep` 命令挂载到 `ssh` 子命令并同步文档示例。
- [x] 优化 Linux 平台 SSH 服务重启逻辑，兼容 `ssh` 服务名称并提供准确提示。
- [x] 修正 `ssh password-login` 状态检测，确保与实际配置一致。
- [x] 修复 Ubuntu 24 下 SSH 端口修改未生效的问题，兼容 Include 配置。
- [x] 增加端口调试日志并完善 drop-in 写入逻辑。
- [x] 调整端口配置策略：使用 drop-in 时仅保留单一 Port 定义。
- [x] 扩充端口调试输出并强制 drop-in 端口覆盖，确保最终生效。
- [x] 改为注释已有 Port 配置（保留原行），避免直接删除。
- [x] 为 `ssh port` 命令增加 `-y/--yes` 自动确认参数。
### 2025-11-02 15:33
- [x] 梳理 `notify email` 环境变量需求并确认参数映射。
- [x] 为 `notify email` 命令添加环境变量读取逻辑及调试日志。
- [x] 补充/更新文档说明环境变量使用方法。
- [x] 编写或更新测试覆盖环境变量场景（如可行）。
- [x] 本地验证并提交代码变更。
### 2025-11-02 15:52
- [x] 复现并分析 `notify email` 阻塞原因，收集必要日志。
- [x] 优化邮件发送逻辑，引入超时及隐式 TLS 支持，避免长时间阻塞。
- [x] 根据变更更新/新增测试与调试日志。
- [x] 验证命令执行及文档同步需求（如有）。
- [x] 提交相关修改。
### 2025-11-02 16:24
- [x] 复现 `ssh watch` 首次启动重放历史登录的问题。
- [x] 调整 journald 监听逻辑，确保首次启动仅追踪新事件。
- [x] 补充必要测试/日志并验证行为。
- [x] 更新文档或说明（如需）。
- [x] 提交修复补丁。
### 2025-11-02 16:28
- [ ] 设计 `sweep` 命令 `--notify` 参数（默认关闭），明确输出行为。
- [ ] 实现参数解析与逻辑分支，默认仅打印事件。
- [ ] 更新相关测试/新增测试覆盖通知开关。
- [ ] 更新文档说明新参数。
- [ ] 验证并提交变更。
